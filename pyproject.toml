[project]
name = "planner-council"
version = "0.1.0"
description = "Multi-agent planning council (local Ollama + plan.md export)"
readme = "README.md"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"
